version: '3.5'
services:
  accurev-client:
    privileged: true
    build: .
    container_name: accurev-client
    volumes:
      - type: bind
        source: var/run/docker.sock
        target: /var/run/docker.sock